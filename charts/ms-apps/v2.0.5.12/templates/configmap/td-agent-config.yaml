kind: ConfigMap
apiVersion: v1
metadata:
  name: td-agent-config
  labels:
    addonmanager.kubernetes.io/mode: Reconcile
data:
  {{- $root := . }}
  {{- range $path, $bytes := .Files.Glob "config/td-agent/*.conf" }}
  {{ base $path }}: |-
  {{- $root.Files.Get $path | nindent 4 }}
  {{- end }}

